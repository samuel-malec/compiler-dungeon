#pragma once

namespace dungeon
{
    // TOOD: report the time it took to run individual compiler phases : lexing, parsing, conversion to IR, analyses, etc...
    struct progress_reporter
    {

    };
}

#pragma once

// TODO; generic dataflow driver -> liveness -> reg2mem -> minimal backend
namespace dungeon {

    // TODO: how to create dataflow driver -> check other implementations
    struct dataflow_driver{};
}
